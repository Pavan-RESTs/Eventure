import 'package:flutter/material.dart';

class EventModificationPage extends StatelessWidget {
  const EventModificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Event Modification Page'),
      ),
    );
  }
}
